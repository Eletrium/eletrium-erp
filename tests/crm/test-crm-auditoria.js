const fs = require('fs');
const vm = require('vm');
const path = require('path');
const SRC = fs.readFileSync(path.resolve(__dirname, '..', '..', 'crm-auditoria.js'), 'utf8');
let pass=0,fail=0; const check=(n,c,d)=>{if(c){pass++;console.log('OK   '+n)}else{fail++;console.log('FAIL '+n+(d?' -- '+d:''))}};
function makeSandbox(opts={}){const created=[];const state={items:(opts.items||[]).slice(),created};const columns=opts.columns||[{name:'Title'},{name:'Origem_Lead'},{name:'Dados_Brutos'},{name:'RazaoSocial_Nome'},{name:'Chave_Dedupe',enforceUniqueValues:true},{name:'Canal_Origem'},{name:'Origem_ID'}];const sandbox={console,Date,window:{},EG:{async listColumns(){return columns},async listItems(){return state.items},async createItem(list,fields){created.push({list,fields});const item={id:String(100+created.length),fields:{...fields}};state.items.push(item);return item}}};vm.createContext(sandbox);vm.runInContext(SRC,sandbox,{filename:'crm-auditoria.js'});return{api:sandbox.window.CRMAuditoria,state}}
(async()=>{const{api,state}=makeSandbox();const pure=api.assertRegras();check('1 regras puras verdes',pure.ok===true&&pure.total>=5,JSON.stringify(pure));const schema=await api.inspecionarSchema();check('2 schema mínimo aceito',schema.ok===true&&schema.faltando.length===0);const lead={origem:'Indicação',razao_social_nome:'Empresa Teste',documento:'11.222.333/0001-81',contato_nome:'Maria',email:'maria@teste.com.br'};const r1=await api.registrar(lead,{meta:{produtor:'teste'}});check('3 primeiro registro cria',r1.criado===true&&state.created.length===1,JSON.stringify(r1));const r2=await api.registrar({...lead,razao_social_nome:'Grafia Diferente'});check('4 mesmo documento não duplica',r2.duplicado===true&&state.created.length===1,JSON.stringify(r2));const env=JSON.parse(state.created[0].fields.Dados_Brutos);check('5 envelope preserva contato',env.lead.contato_nome==='Maria'&&env.lead.email==='maria@teste.com.br');const missing=makeSandbox({columns:[{name:'Origem_Lead'}]});let blocked=false;try{await missing.api.registrar(lead)}catch(e){blocked=!!e.failClosed}check('6 schema incompleto bloqueia',blocked&&missing.state.created.length===0);check('7 não cria Proposta/Cliente',!/createItem\s*\(\s*["'](?:Propostas|Clientes)["']/.test(SRC));console.log(`\nCRM-D core: ${pass} PASS / ${fail} FAIL`);if(fail)process.exitCode=1})().catch(e=>{console.error(e);process.exitCode=1});
